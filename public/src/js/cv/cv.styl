.content 
    .page
        .cv
            position relative
            margin-left auto
            margin-right auto
            max-width 800px
            min-height 600px
            .box
                background-color rgba(255,255,255,0.3)
                display block
                position: relative;
                border-radius: 5px;
                margin-bottom: 5px;
                box-shadow: 0 1px 1px rgba(0, 0, 0, 0.4);
